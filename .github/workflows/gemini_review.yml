name: Gemini Reviewer

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Gemini Review
        # Đây là công cụ (Action) đã được kiểm chứng hoạt động tốt
        uses: stwise/gemini-review-action@v0.1.2
        with:
          api_key: ${{ secrets.GEMINI_API_KEY }}
          model: "gemini-1.5-flash"
          instructions: "Hãy review code bằng tiếng Việt. Tập trung vào lỗi logic, bảo mật và hiệu năng cho dự án web phim."
